% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anon.R
\name{anonymize_data}
\alias{anonymize_data}
\title{Anonymize a data frame with key-based reversible transforms}
\usage{
anonymize_data(data, key, columns = NULL, opts = list())
}
\arguments{
\item{data}{A data.frame.}

\item{key}{Secret key: raw bytes (e.g. from \code{generate_key()}) or character
passphrase. The key drives the rotation, centering, scaling, and
encryption — different keys produce different anonymized output.}

\item{columns}{Character vector of column names to anonymize, or NULL to
anonymize all columns.}

\item{opts}{Named list of options:
\describe{
\item{method}{Transformation method: \code{"pca"} (default) or
\code{"cryptoencoder"}.}
\item{ae_max_epochs}{Maximum training epochs for the cryptoencoder
(default 5000). Ignored for \code{"pca"}.}
\item{ae_tol}{Early-stopping tolerance for cryptoencoder training loss
(default 1e-10). Ignored for \code{"pca"}.}
\item{ae_lr}{Learning rate for cryptoencoder training (default 0.01).
Ignored for \code{"pca"}.}
}}
}
\value{
List with \code{data} (anonymized data.frame) and \code{mapping} (metadata
needed for reversal). For \code{"pca"}, output has one fewer column; for
\code{"cryptoencoder"}, output has the same number of columns.
}
\description{
All selected columns are encoded to numeric (categorical to label codes,
dates to epoch values), then transformed jointly. The default method
(\code{"pca"}) uses a key-dependent orthogonal rotation and drops one column;
\code{"cryptoencoder"} trains a shallow n-to-n autoencoder where every output
depends nonlinearly on all inputs, preserving the column count.
}
