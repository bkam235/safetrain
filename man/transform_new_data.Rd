% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anon.R
\name{transform_new_data}
\alias{transform_new_data}
\title{Apply an existing anonymization mapping to new data}
\usage{
transform_new_data(new_data, key, mapping)
}
\arguments{
\item{new_data}{A data.frame with the same feature columns that were
anonymized when \code{mapping} was created.}

\item{key}{The same secret key used to create \code{mapping}.}

\item{mapping}{The \code{mapping} list returned by \code{\link[=anonymize_data]{anonymize_data()}}.}
}
\value{
A data.frame with the same layout as \code{anonymize_data()$data}:
non-anonymized columns preserved, anonymized columns replaced by
\code{anon_1, ..., anon_n}.
}
\description{
Transforms new observations using the encoder trained during a previous
call to \code{\link[=anonymize_data]{anonymize_data()}}.  Unlike \code{\link[=anonymize_data]{anonymize_data()}}, no autoencoder is
retrained: the encrypted weights stored in \code{mapping} are decrypted and
applied directly, so the new data lands in the same representation space as
the original anonymized data.
}
\details{
This is the correct pattern for production use: anonymize a training set
with \code{\link[=anonymize_data]{anonymize_data()}}, then pass the returned \code{mapping} here to
transform held-out or streaming data consistently.

Only \code{method = "cryptoencoder"} mappings are supported.  For
\code{method = "pca"} the key-derived rotation is already fully
data-independent, so calling \code{\link[=anonymize_data]{anonymize_data()}} directly on new data already
produces a consistent representation.
}
